name: lint, format, compile

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened] # PRが作成・更新・再オープンされたとき
    branches: [ main ]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'node'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run lint and format
        run: npm run ci

      - name: Run compile
        run: npm run compile
